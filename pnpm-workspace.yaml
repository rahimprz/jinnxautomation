# Packages whose install scripts may run. sharp backs Next.js image
# optimization, esbuild backs drizzle-kit, unrs-resolver backs
# eslint-config-next, fsevents is the macOS file watcher.
allowBuilds:
  esbuild: true
  fsevents: true
  sharp: true
  unrs-resolver: true
